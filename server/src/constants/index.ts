const ORIGIN = "*";
const DATABASE_URL = "";
const PORT = "3000";
const NODE_ENV = process.env.NODE_ENV || "development";
const JWT_SECRET = "secret";

export { ORIGIN, DATABASE_URL, PORT, NODE_ENV, JWT_SECRET };
